---
title: IconButton
---

# IconButton

```jsx live
<IconButton
  size="md"
  IconComponent={Icon.Play.Fill}
  ariaLabel="Play"
/>
```

## How to use

```jsx
import { IconButton } from '@vibrant-ui/components';
```

## Properties

| Prop          | Type                                                   | Default | Description                           |
| ------------- | ------------------------------------------------------ | ------- | ------------------------------------- |
| [size](#크기) | `lg` \| `md` \| `sm`                                   |         | 버튼의 크기                           |
| color         | [OnColorToken](/docs/core-concepts/colors/color-token) |         | 버튼 텍스트, 아이콘의 색상            |
| type          | `button` \| `submit `                                  |         | `button` 요소의 타입                  |
| IconComponent | `IconComponent<IconProps, Fill \| Regular \| Thin>`    |         | 버튼에 표시되는 아이콘                |
| disabled      | `boolean`                                              |         | 버튼의 비활성화 여부                  |
| onClick       | `() => void`                                           |         | 버튼이 클릭됐을 때 호출되는 콜백 함수 |
| ariaLabel     | `string`                                               |         | 버튼의 접근 가능한 이름               |

## Usage

### 크기

```jsx live
<HStack spacing={24} alignVertical="center">
  <IconButton size="lg" IconComponent={Icon.Play.Fill} />
  <IconButton size="md" IconComponent={Icon.Play.Fill} />
  <IconButton size="sm" IconComponent={Icon.Play.Fill} />
</HStack>
```
